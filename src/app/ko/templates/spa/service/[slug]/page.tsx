import ServiceDetailFull from "./ServiceDetailFull";

// Sample detail page - every service card on /service links to this same
// sample treatment regardless of slug, mirroring the blog/[slug] pattern.
export default function ServiceDetailPage() {
  return <ServiceDetailFull />;
}

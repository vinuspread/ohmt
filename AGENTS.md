# 프로젝트 작업 지침

## 템플릿 화면 캡처

- 템플릿 UI를 새로 만들거나 수정한 작업은 완료 보고 전에 캡처본까지 생성한다.
- 별도 요청이 없어도 변경한 템플릿의 한국어·영어 페이지를 데스크톱과 모바일 화면으로 모두 캡처한다.
- 인스타그램 게시 준비용 캡처는 `output/playwright/instagram-templates/` 한 폴더에 모은다.
- 파일명은 언어, 템플릿 번호와 이름, 페이지 경로, 화면 크기를 바로 알 수 있게 유지한다.
- 기본 캡처 명령은 `npm run capture:templates`이다. 특정 템플릿만 작업했다면 `--templates OHMT001-fashion,OHMT002-jewelry`를 덧붙인다.
- 전체 페이지 요청에는 `--all-pages`를 사용한다. 기본 명령은 각 템플릿의 대표 페이지를 캡처한다.
- 캡처가 끝나면 폴더의 `manifest.json`에서 성공·실패·누락 수를 확인하고, 실패가 있으면 원인을 해결한 뒤 다시 실행한다.
- 동적 주소(`[id]`, `[slug]`)는 실제 값이 필요한 페이지이므로 자동 캡처 대상에서 제외될 수 있다. 실제 링크를 확인할 수 있으면 구체 주소로 추가 캡처한다.
